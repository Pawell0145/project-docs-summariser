<Window x:Class="WpfAiIntegration.CreatePlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Study Plan" Height="550" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="#E0E0E0" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CREATE NEW PLAN" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,25" Foreground="#007ACC"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="SUBJECT" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="12" FontWeight="SemiBold"/>
                <TextBox x:Name="TopicTextBox" Height="32"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                <TextBlock Text="DAYS" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="12" FontWeight="SemiBold"/>
                <TextBox x:Name="DaysTextBox" Height="32" PreviewTextInput="NumberValidationTextBox"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="HOURS" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="12" FontWeight="SemiBold"/>
                <TextBox x:Name="HoursTextBox" Height="32" PreviewTextInput="NumberValidationTextBox"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="SOURCE (Files)" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="12" FontWeight="SemiBold"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="0,0,10,0" Background="#2D2D30" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="5">
                    <ListBox x:Name="FilesListBox" Height="70" Background="Transparent" Foreground="White" BorderThickness="0"/>
                </Border>
                <Button Grid.Column="1" Content="+" Width="35" Height="35" VerticalAlignment="Top" Click="SelectFiles_Click" FontSize="18"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <TextBlock Text="NOTES / PREFERENCES" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="12" FontWeight="SemiBold"/>
            <TextBox x:Name="NotesTextBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="120"/>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Button Content="CANCEL" Width="110" Height="38" HorizontalAlignment="Left" Click="Cancel_Click" Background="#4A4A4A"/>
            <Button Content="CREATE" Width="110" Height="38" HorizontalAlignment="Right" Background="#28A745" Foreground="White" FontWeight="Bold" Click="CreatePlan_Click"/>
        </Grid>
    </Grid>
</Window>