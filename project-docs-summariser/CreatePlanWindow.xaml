<Window x:Class="WpfAiIntegration.CreatePlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Study Plan" Height="580" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="#E0E0E0" FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#2D2D30"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#555555"/>
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#0098FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#3A3A3A"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TxtBorder" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="TxtBorder" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid x:Name="MainContent" Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="CREATE NEW STUDY PLAN" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#007ACC"/>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.5*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="SUBJECT" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="11" FontWeight="Bold"/>
                    <TextBox x:Name="TopicTextBox" Height="34" ToolTip="E.g. Biology, C# Programming..."/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,15,0">
                    <TextBlock Text="DAYS" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="11" FontWeight="Bold"/>
                    <TextBox x:Name="DaysTextBox" Height="34" PreviewTextInput="NumberValidationTextBox" HorizontalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="HOURS/DAY" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="11" FontWeight="Bold"/>
                    <TextBox x:Name="HoursTextBox" Height="34" PreviewTextInput="NumberValidationTextBox" HorizontalContentAlignment="Center"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Margin="0,0,0,20">
                <TextBlock Text="SOURCE DOCUMENTS" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="11" FontWeight="Bold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Margin="0,0,15,0" Background="#252526" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="4">
                        <ListBox x:Name="FilesListBox" Height="80" Background="Transparent" Foreground="#D4D4D4" BorderThickness="0" Padding="5" KeyDown="FilesListBox_KeyDown" ToolTip="Select the file and press the 'Delete' key to delete it"/>
                    </Border>
                    <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="+" Width="34" Height="34" VerticalAlignment="Top" Click="SelectFiles_Click" FontSize="20" ToolTip="Add PDF, DOCX, or PPTX"/>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,0,0,20">
                <TextBlock Text="ADDITIONAL NOTES (Optional)" Margin="0,0,0,5" Foreground="#AAAAAA" FontSize="11" FontWeight="Bold"/>
                <TextBox x:Name="NotesTextBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="110"/>
            </StackPanel>

            <Grid Grid.Row="4" Margin="0,15,0,0">
                <Button x:Name="CancelBtn" Content="CANCEL" Style="{StaticResource ModernButton}" Width="120" Height="40" HorizontalAlignment="Left" Click="Cancel_Click"/>
                <Button x:Name="CreateBtn" Content="GENERATE PLAN" Style="{StaticResource PrimaryButton}" Width="160" Height="40" HorizontalAlignment="Right" Click="CreatePlan_Click"/>
            </Grid>
        </Grid>

        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Background="#D81E1E1E">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⚙️ Generating Syllabus..." FontSize="24" FontWeight="Bold" Foreground="#007ACC" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="AI is analyzing your documents. Please wait." FontSize="14" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>