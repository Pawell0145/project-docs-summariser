<Window x:Class="WpfAiIntegration.CreatePlanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Study Plan" Height="550" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="#E0E0E0" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CREATE NEW PLAN" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#007ACC"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="SUBJECT" Margin="0,0,0,5" Foreground="#AAAAAA"/>
                <TextBox x:Name="TopicTextBox" Height="30"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                <TextBlock Text="DAYS" Margin="0,0,0,5" Foreground="#AAAAAA"/>
                <TextBox x:Name="DaysTextBox" Height="30" PreviewTextInput="NumberValidationTextBox"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="TIME" Margin="0,0,0,5" Foreground="#AAAAAA"/>
                <TextBox x:Name="HoursTextBox" Height="30" PreviewTextInput="NumberValidationTextBox"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="SOURCE (Files)" Margin="0,0,0,5" Foreground="#AAAAAA"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ListBox x:Name="FilesListBox" Grid.Column="0" Height="70" Margin="0,0,10,0" Background="#2D2D30" Foreground="White" BorderBrush="#3F3F46"/>
                <Button Grid.Column="1" Content="..." Width="35" Height="30" VerticalAlignment="Top" Click="SelectFiles_Click" Background="#3F3F46"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="NOTES / PREFERENCES" Margin="0,0,0,5" Foreground="#AAAAAA"/>
            <TextBox x:Name="NotesTextBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="100"/>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Button Content="CANCEL" Width="100" Height="35" HorizontalAlignment="Left" Click="Cancel_Click" Background="#4A4A4A"/>
            <Button Content="CREATE" Width="100" Height="35" HorizontalAlignment="Right" Background="#28A745" Foreground="White" FontWeight="Bold" Click="CreatePlan_Click"/>
        </Grid>
    </Grid>
</Window>