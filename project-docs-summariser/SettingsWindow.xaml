<Window x:Class="project_docs_summariser.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="700" Width="650"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#121212" Foreground="#E0E0E0" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0098FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <TextBlock Text="SETTINGS" FontSize="32" FontWeight="Black" Foreground="#007ACC" Margin="0,0,0,30"/>

            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="AI CONNECTION" FontSize="14" FontWeight="Bold" Foreground="#888888" Margin="0,0,0,15"/>
                    <TextBlock Text="Groq API Key" Foreground="#D4D4D4" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <PasswordBox x:Name="ApiKeyInput" Grid.Column="0" Height="40" Background="#2D2D30" Foreground="White" BorderThickness="1" BorderBrush="#3F3F46" Padding="12,0" VerticalContentAlignment="Center" Margin="0,0,15,0"/>
                        <Button Content="SAVE KEY" Grid.Column="1" Height="40" Click="SaveKey_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="INTERFACE &amp; ANIMATIONS" FontSize="14" FontWeight="Bold" Foreground="#888888" Margin="0,0,0,20"/>

                    <CheckBox x:Name="DisableAnimationsCheckBox"
                              Content="Disable Typing Animations"
                              FontSize="14"
                              Margin="0,0,0,25"
                              Foreground="#E0E0E0"
                              Click="DisableAnimationsCheckBox_Click"/>

                    <TextBlock Text="AI Typing Speed (milliseconds per letter)" Foreground="#D4D4D4" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="AnimationSpeedSlider" 
                                Grid.Column="0"
                                Minimum="1" Maximum="50" 
                                TickFrequency="1" IsSnapToTickEnabled="True" 
                                ValueChanged="AnimationSpeedSlider_ValueChanged"
                                VerticalAlignment="Center" Margin="0,0,20,0"/>
                        <TextBlock x:Name="AnimationSpeedValueText" Grid.Column="1" Text="5 ms" Foreground="#007ACC" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SettingsCardStyle}">
                <StackPanel>
                    <TextBlock Text="PERSONALIZATION" FontSize="14" FontWeight="Bold" Foreground="#888888" Margin="0,0,0,20"/>

                    <StackPanel Margin="0,0,0,30">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="AI Content Language" Foreground="#D4D4D4" FontWeight="SemiBold" FontSize="14"/>
                            <Border Background="#FF8C00" CornerRadius="4" Padding="5,1" Margin="10,0,0,0">
                                <TextBlock Text="BETA" FontSize="10" FontWeight="Bold" Foreground="#121212"/>
                            </Border>
                        </StackPanel>
                        <ComboBox x:Name="LanguageCombo" Width="250" Height="35" HorizontalAlignment="Left" 
          FontSize="14" FontWeight="SemiBold"
          SelectionChanged="LanguageCombo_SelectionChanged">

                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="Background" Value="#2D2D30"/>
                                    <Setter Property="Foreground" Value="#E0E0E0"/>
                                    <Setter Property="BorderBrush" Value="#3F3F46"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBox">
                                                <Grid>
                                                    <ToggleButton x:Name="ToggleButton" 
                                          Background="{TemplateBinding Background}" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          Focusable="false" 
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                                        <ToggleButton.Template>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="4">
                                                                    <Path x:Name="Arrow" Fill="#E0E0E0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </ToggleButton.Template>
                                                    </ToggleButton>

                                                    <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                              Margin="12,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left"
                                              TextBlock.Foreground="#E0E0E0"/>

                                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                        <Border x:Name="DropDownBorder" Background="#1E1E1E" BorderThickness="1" BorderBrush="#3F3F46" CornerRadius="4" MinWidth="{TemplateBinding ActualWidth}">
                                                            <ScrollViewer Margin="0,4,0,4" SnapsToDevicePixels="True">
                                                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                            </ScrollViewer>
                                                        </Border>
                                                    </Popup>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.Style>

                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Foreground" Value="#E0E0E0"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Background" Value="#1E1E1E"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ComboBoxItem">
                                                <Border Name="ItemBorder" Background="{TemplateBinding Background}" BorderThickness="0">
                                                    <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>

                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#1E1E1E"/>
                                                        <Setter Property="Foreground" Value="#007ACC"/>
                                                    </Trigger>

                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#007ACC"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </Trigger>

                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="IsSelected" Value="True"/>
                                                            <Condition Property="IsMouseOver" Value="True"/>
                                                        </MultiTrigger.Conditions>
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#007ACC"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </MultiTrigger>

                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.ItemContainerStyle>

                            <ComboBoxItem Content="English" Tag="English"/>
                            <ComboBoxItem Content="Polski" Tag="Polish"/>
                        </ComboBox>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Keywords Highlight" Foreground="#D4D4D4" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,10"/>
                            <WrapPanel x:Name="KeywordColorPalette" Width="140" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Sentences Highlight" Foreground="#D4D4D4" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,10"/>
                            <WrapPanel x:Name="SentenceColorPalette" Width="140" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Window>